/**
 * Toggle off to hide Projects / Blogs in the site navigation (and related 404 CTAs).
 * Set back to `true` when you want those sections discoverable again.
 */
export const SHOW_PROJECTS_AND_BLOGS_NAV = false;
